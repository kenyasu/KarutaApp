<%= form_with(model: game_count, local: true) do |form| %>
  <% if game_count.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_count.errors.count, "error") %> prohibited this game_count from being saved:</h2>

      <ul>
      <% game_count.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
