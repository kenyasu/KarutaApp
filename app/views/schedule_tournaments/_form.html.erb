<%= form_with(model: schedule_tournament, local: true) do |form| %>
  <% if schedule_tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule_tournament.errors.count, "error") %> prohibited this schedule_tournament from being saved:</h2>

      <ul>
      <% schedule_tournament.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :tournament_id %>
    <%= form.number_field :tournament_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
