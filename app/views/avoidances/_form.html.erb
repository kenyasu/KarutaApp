<%= form_with(model: avoidance, local: true) do |form| %>
  <% if avoidance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(avoidance.errors.count, "error") %> prohibited this avoidance from being saved:</h2>

      <ul>
      <% avoidance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :result_id %>
    <%= form.number_field :result_id %>
  </div>

  <div class="field">
    <%= form.label :karuta_id %>
    <%= form.number_field :karuta_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
